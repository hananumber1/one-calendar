{
  "name": "one-calendar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    /*개발 서버 구동 (localhost:****)*/
    "dev": "nuxt",
    /*배포를 목적으로 애플리케이션을 빌드합니다. (CSS, JS 압축 등) .nuxt에 파일 생성*/
    "build": "nuxt build",
    /*배포 모드로 서버를 구동합니다. (빌드 명령 후에 사용)*/
    "start": "nuxt start",
    /*정적 호스팅을 목적으로 애플리케이션을 빌드 합니다. (모든 라우트의 HTML 파일 생성) dist에 파일 생성*/
    "generate": "nuxt generate",
    "test": "jest"
  },
  "dependencies": {
    "@nuxtjs/bulma": "^1.3.0",
    "core-js": "^3.6.5",
    "nuxt": "^2.14.5"
  },
  "devDependencies": {
    "@vue/test-utils": "^1.1.0",
    "babel-core": "7.0.0-bridge.0",
    "babel-jest": "^26.3.0",
    "jest": "^26.4.2",
    "vue-jest": "^3.0.4"
  }
}
